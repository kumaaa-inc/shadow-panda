import { Tabs, Tab, Steps, Callout } from 'nextra/components'
import { css } from '@shadow-panda/styled-system/css'
import { Preview } from '@/components/_docs/preview'
import { ReferenceBadges } from '@/components/_docs/reference-badges'
import { Lead } from '@/components/_docs/lead'
import { Label } from '@/components/ui/label'
import { Button } from '@/components/ui/button'
import { Textarea } from '@/components/ui/textarea'

# Textarea

<Lead>
  {'Displays a form textarea or a component that looks like a textarea.'}
</Lead>

<ReferenceBadges shadcnUiLink="https://ui.shadcn.com/docs/components/textarea" />

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <Textarea placeholder="Type your message here." />
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return <Textarea placeholder="Type your message here." />
}
```

  </Tab>
</Tabs>

## Installation

<Steps>
### Copy and paste the following code into your project

```tsx filename="components/ui/textarea.tsx" copy
import * as React from 'react'
import { cx } from '@shadow-panda/styled-system/css'
import { textarea } from '@shadow-panda/styled-system/recipes'

export interface TextareaProps
  extends React.TextareaHTMLAttributes<HTMLTextAreaElement> {}

const Textarea = React.forwardRef<HTMLTextAreaElement, TextareaProps>(
  ({ className, ...props }, ref) => {
    return (
      <textarea className={cx(textarea(), className)} ref={ref} {...props} />
    )
  },
)

Textarea.displayName = 'Textarea'

export { Textarea }
```

### Update the import paths to match your project setup

</Steps>

## Usage

```tsx copy
import { Textarea } from '@/components/ui/textarea'
```

```tsx copy
<Textarea />
```

## Examples

### Default

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <Textarea placeholder="Type your message here." />
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return <Textarea placeholder="Type your message here." />
}
```

  </Tab>
</Tabs>

### Disabled

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <Textarea placeholder="Type your message here." disabled />
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return <Textarea placeholder="Type your message here." disabled />
}
```

  </Tab>
</Tabs>

### With Label

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <div className={css({
        display: 'grid',
        w: 'full',
        gap: '1.5'
      })}>
        <Label htmlFor="message">Your message</Label>
        <Textarea id="message" placeholder="Type your message here." />
      </div>
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return (
    <div
      className={css({
        display: 'grid',
        w: 'full',
        gap: '1.5',
      })}
    >
      <Label htmlFor="message">Your message</Label>
      <Textarea id="message" placeholder="Type your message here." />
    </div>
  )
}
```

  </Tab>
</Tabs>

### With Text

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <div className={css({
        display: 'grid',
        w: 'full',
        gap: '1.5'
      })}>
        <Label htmlFor="message-2">Your message</Label>
        <Textarea id="message-2" placeholder="Type your message here." />
        <p className={css({textStyle: 'sm', color: 'muted.foreground'})}>Your message will be copied to the support team.</p>
      </div>
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return (
    <div
      className={css({
        display: 'grid',
        w: 'full',
        gap: '1.5',
      })}
    >
      <Label htmlFor="message-2">Email</Label>
      <Textarea id="message-2" placeholder="Type your message here." />
      <p className={css({ textStyle: 'sm', color: 'muted.foreground' })}>
        Your message will be copied to the support team.
      </p>
    </div>
  )
}
```

  </Tab>
</Tabs>

### With Button

<Tabs items={['Preview', 'Code']}>
  <Tab>
    <Preview>
      <div className={css({
        display: 'grid',
        w: 'full',
        gap: '2'
      })}>
        <Textarea placeholder="Type your message here." />
        <Button>Send message</Button>
      </div>
    </Preview>
  </Tab>
  <Tab>

```tsx copy
import { Button } from '@/components/ui/button'
import { Textarea } from '@/components/ui/textarea'

export function Example() {
  return (
    <div
      className={css({
        display: 'grid',
        w: 'full',
        gap: '2',
      })}
    >
      <Textarea placeholder="Type your message here." />
      <Button>Send message</Button>
    </div>
  )
}
```

  </Tab>
</Tabs>

### Form

<Callout type="warning" emoji="⚒️">
  WIP
</Callout>
